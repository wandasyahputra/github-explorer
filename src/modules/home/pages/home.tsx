import SearchComponent from "../features/search.component";

const Home = () => {
  return (
    <div className="w-full h-full flex items-center">
      <SearchComponent />
    </div>
  );
};

export default Home;
